# תצורת Fail2Ban למערכת Supabase
# קובץ זה יועתק ל-/etc/fail2ban/jail.local

[DEFAULT]
bantime = 3600
findtime = 600
maxretry = 5

[sshd]
enabled = true
port = SSH_PORT_PLACEHOLDER
logpath = /var/log/auth.log

[nginx-http-auth]
enabled = true
port = http,https
logpath = /var/log/nginx/error.log
